sieve = [i for i in range(2, int(input())+1)]
for i in sieve:
    for j in sieve:
        if sieve[sieve.index(j)] % i == 0 and sieve.index(j) >= i:
            del sieve[sieve.index(j)]
print(sieve)
